/**
 * settings
 *
 * @param {boolean} bool boolean argument
 * @param {string} str string argument
 * @param {number} num number argument
 * @returns {object}
 */
function settings(bool, str, num) {
  return {};
}

/**
 * jsdoc/check-examples
 *
 * @return {Boolean}
 * @example
 *  checkExamples();
 * @example  checkExamples();
 */
function checkExamples() {
  return true;
}

/**
 * jsdoc/check-syntax
 *
 * @param {String=} arg1 argument 1
 * @return {Boolean}
 */
function checkSyntax(arg1) {
  return true;
}

/**
 *
 */
function requireDescriptionNotSet() {
  // has no settings
}

/**
 * @description jsdoc/require-description
 */
function requireDescriptionUseTag() {
  // has no settings
}
